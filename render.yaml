services:
  - type: web
    name: cofure
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: TZ
        value: Asia/Ho_Chi_Minh
      - key: ENV
        value: production
